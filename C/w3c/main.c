#include <stdio.h>

struct Node
{
    int value;
    struct Node* nextNode;
};

void nodeIncrement(struct* actualNode) {

}

int main() {
    int value;
   printf("Enter nodes values");
   for (i = 0; i < 3; i++)
    {
        scanf("%d" &value);
        
    }
   return 0;
}